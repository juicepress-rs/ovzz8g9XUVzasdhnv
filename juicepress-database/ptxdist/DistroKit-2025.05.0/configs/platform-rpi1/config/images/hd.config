image @IMAGE@ {
	hdimage {
		align = 1M
		partition-table-type = hybrid
	}
	partition boot-vfat {
		image = boot.vfat
		partition-type-uuid = fat32
		partition-type = 0xc
		bootable = true
	}
	partition root-A {
		image = root.ext2
		partition-type-uuid = root-arm
	}
}
